import React, { FC } from 'react';
import { IBlock } from '../../../libs/types';

interface IProps {
  rightData: IBlock[];
}

export const RightBlock: FC<IProps> = ({ rightData }) => {
  return (
    <>

    </>
  );
};
